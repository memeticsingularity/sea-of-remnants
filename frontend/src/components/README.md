# src/components/

React 组件目录。

## 子目录

- `layout/` — 布局组件（AppShell、SidebarNav、TopBar）
- `ui/` — 基础 UI 组件（Card、Tag、StatBlock、Breadcrumb）
- `search/` — 搜索相关组件
- `glossary/` — 术语提示组件
- `cards/` — 列表卡片组件

## 新增组件

1. 判断组件用途，选择对应子目录。
2. 组件文件使用 PascalCase。
3. 在需要的地方使用 TypeScript 类型。
